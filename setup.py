from setuptools import setup, find_packages
setup(name='irws_watcher',
      version='1.0',
      description='UW-IT IRWS message processor for affiliations and pacs',
      packages=find_packages(),
      include_package_data=True,
     )

